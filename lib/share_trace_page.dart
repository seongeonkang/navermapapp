import 'package:flutter/material.dart';

class ShareTracePage extends StatelessWidget {
  const ShareTracePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('흔적공유 페이지'),
    );
  }
}
